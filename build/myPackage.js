//@ts-check
/**
 * Initializes the project
 * @param {object} config
 * @returns {boolean}
 */
export function init(config) {
    return true;
}
/**
 * return exit code
 * @param {number} code
 * @returns {number}
 */
export function exit(code) {
    return code + 1;
}
